<?xml version="1.0" encoding="UTF-8"?>
<!--
  Mobile Agent — headless desktop agent (macOS launchd LaunchAgent).

  Install:
    cp com.contextsolutions.mobileagent.plist ~/Library/LaunchAgents/
    launchctl load -w ~/Library/LaunchAgents/com.contextsolutions.mobileagent.plist
    # logs: tail -f /tmp/mobileagent.out /tmp/mobileagent.err
  Stop / remove:
    launchctl unload -w ~/Library/LaunchAgents/com.contextsolutions.mobileagent.plist
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.contextsolutions.mobileagent</string>
    <key>ProgramArguments</key>
    <array>
        <string>/Applications/MobileAgent.app/Contents/MacOS/MobileAgent</string>
    </array>
    <key>EnvironmentVariables</key>
    <dict>
        <key>MOBILEAGENT_HEADLESS</key>
        <string>1</string>
    </dict>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>StandardOutPath</key>
    <string>/tmp/mobileagent.out</string>
    <key>StandardErrorPath</key>
    <string>/tmp/mobileagent.err</string>
</dict>
</plist>
